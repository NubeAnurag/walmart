{
  "name": "walmart-digital-revolution",
  "version": "1.0.0",
  "description": "Walmart Digital Revolution - Full Stack Web Application",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\"",
    "start": "cd backend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [
    "walmart",
    "retail",
    "inventory",
    "management",
    "react",
    "nodejs",
    "mongodb"
  ],
  "author": "Anurag Mandal",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
